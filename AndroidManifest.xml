<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.umn.pinkpanthers.beerfinder"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/MnTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchByZipActivity"
            android:label="@string/search_by_zip_label"
            android:theme="@style/MnTheme" >
        </activity>
        <activity
            android:name=".SearchByBrandActivity"
            android:label="@string/search_by_brand_label"
            android:theme="@style/MnTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ContactDetailActivity"
            android:label="@string/app_name"
            android:theme="@style/MnTheme" >
        </activity>
        <activity
            android:name="ContactEditActivity"
            android:label="@string/app_name"
            android:theme="@style/MnTheme" >
        </activity>
        <activity
            android:name="edu.umn.pinkpanthers.beerfinder.ContactNewActivity"
            android:label="@string/app_name"
            android:theme="@style/MnTheme" >
        </activity>
    </application>

</manifest>